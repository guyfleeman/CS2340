machine:
  java:
    version: oraclejdk8

dependencies:
  pre:
    - add-apt-repository ppa:cwchien/gradle
    - apt-get update
    - apt-get install gradle

deployment:
  github:
    branch: master
    commands:
      - gradle checkstyleMain --stacktrace
